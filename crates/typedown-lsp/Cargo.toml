[package]
name = "typedown-lsp"
version = "0.1.0"
edition = "2024"

[dependencies]
typedown-db = { version = "0.1.0", path = "../typedown-db" }
typedown-types = { version = "0.1.0", path = "../typedown-types" }
lsp-server = "0.7"
lsp-types = "0.97"
serde_json = "1"
