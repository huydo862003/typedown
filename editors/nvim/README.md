# Typedown for Neovim

LSP client configuration for the Typedown language (`.tdr` files).

## Requirements

`typedown-lsp` must be available on your `PATH`.

## Setup

Add the plugin to your Neovim config, then the LSP will attach automatically when opening `.tdr` files.
