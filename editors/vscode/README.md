# Typedown for VSCode / VSCodium

LSP integration for the Typedown language (`.tdr` files).

## Features

- Syntax highlighting via LSP semantic tokens
- Hover, completions, go-to-definition, diagnostics
