{
  "$schema": "https://json.schemastore.org/package",
  "name": "tree-sitter-tdr",
  "version": "0.0.0",
  "private": true,
  "description": "Typedown TDR grammar for tree-sitter",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/huydo862003/typedown/packages/tree-sitter.git"
  },
  "license": "AGPL",
  "author": {
    "name": "Huy-DNA",
    "email": "huydo862003@gmail.com"
  },
  "keywords": [
    "incremental",
    "parsing",
    "tree-sitter",
    "tdr"
  ],
  "files": [
    "tree-sitter.json",
    "grammar.js",
    "queries/*",
    "src/**",
    "tdr-*/grammar.js",
    "tdr-*/queries/*",
    "tdr-*/src/**",
    "*.wasm"
  ],
  "devDependencies": {
    "tree-sitter-cli": "^0.26.9"
  },
  "scripts": {
    "generate": "bash scripts/generate.sh",
    "build:so": "pnpm run generate && bash scripts/build.sh so",
    "build:wasm": "pnpm run generate && bash scripts/build.sh wasm",
    "build": "pnpm run generate && bash scripts/build.sh all",
    "build:local": "pnpm run build",
    "build:staging": "pnpm run build",
    "test": "bash scripts/test.sh",
    "prestart": "pnpm run build:wasm",
    "start": "tree-sitter playground"
  }
}
